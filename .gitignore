# Build output
bin/
obj/
publish/
publish-ship/

# Career files are runtime state, not source — and they hold your API key if you set one.
# Back them up with Settings -> Data instead.
data/

# IDE / tooling
.vs/
.vscode/
*.user
*.suo
*.userprefs

# OS
Thumbs.db
desktop.ini
.DS_Store

# Manual build output and capture scratch — regenerated by docs/manual/render.py.
# The demo careers under docs/manual/ are runtime state like any other career file.
docs/manual/pages/
docs/manual/pages-operations/
docs/manual/shots/
docs/manual/*.pdf
docs/manual/chromeprof/
docs/manual/printprof/
docs/manual/demodata*/
docs/manual/deddemo/
